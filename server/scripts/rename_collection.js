import mongoose from 'mongoose'; import dotenv from 'dotenv'; dotenv.config({ path: 'server/.env' }); async function run() { await mongoose.connect(process.env.MONGO_URL); console.log('Connected'); try { await mongoose.connection.db.renameCollection('notes', 'spaces'); console.log('Renamed notes to spaces'); } catch (e) { console.error('Error renaming (maybe already renamed or doesnt exist?):', e.message); } process.exit(0); } run();
